plugins {
    id 'java'
    //id 'maven-publish'
}

sourceSets {
    main {
        java {
            srcDirs = ['src/main/']
        }
    }
}

task sourcesJar(type: Jar) {
    from sourceSets.main.allSource
}

/*publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.karrmedia'
            artifactId = 'ftc-hotpatch'
            version = "0.1.0"
            from components.java

            pom {
                name = 'ftc-hotpatch'
            }
        }
    }
}*/